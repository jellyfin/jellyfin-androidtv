@Composable
private fun JellyseerrDetail(
	item: JellyseerrSearchItem,
	details: JellyseerrMovieDetails?,
	requestStatusMessage: String?,
	onRequestClick: () -> Unit,
) {
	val requestButtonFocusRequester = remember { FocusRequester() }

Box(
    modifier = Modifier
        .fillMaxSize()
) {
    // Hintergrund
    AsyncImage(
        modifier = Modifier
            .fillMaxSize()
            .graphicsLayer(alpha = 0.
